services:
  - type: web
    name: pdf-qa-frontend
    env: docker
    branch: main
    autoDeploy: true

    rootDir: frontend2
    dockerContext: frontend2
    dockerfilePath: frontend2/Dockerfile.prod

    healthCheckPath: /
    ports:
      - 80
